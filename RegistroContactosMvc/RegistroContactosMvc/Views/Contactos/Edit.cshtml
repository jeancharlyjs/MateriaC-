@model Contacto

@{
    ViewData["Title"] = "Editar contacto";
}

<section class="form-page">
    <div class="form-heading">
        <span class="eyebrow">Actualizar registro</span>
        <h1>Editar contacto</h1>
        <p>Modifique la informacion y guarde los cambios.</p>
    </div>

    <form asp-action="Edit" method="post" class="contact-form">
        <input type="hidden" asp-for="Id" />
        <div asp-validation-summary="ModelOnly" class="validation"></div>

        <div class="field">
            <label asp-for="Nombre"></label>
            <input asp-for="Nombre" />
            <span asp-validation-for="Nombre"></span>
        </div>

        <div class="field">
            <label asp-for="Telefono"></label>
            <input asp-for="Telefono" />
            <span asp-validation-for="Telefono"></span>
        </div>

        <div class="field">
            <label asp-for="Correo"></label>
            <input asp-for="Correo" />
            <span asp-validation-for="Correo"></span>
        </div>

        <div class="field">
            <label asp-for="Ciudad"></label>
            <input asp-for="Ciudad" />
            <span asp-validation-for="Ciudad"></span>
        </div>

        <div class="field">
            <label asp-for="FechaRegistro"></label>
            <input asp-for="FechaRegistro" type="date" />
            <span asp-validation-for="FechaRegistro"></span>
        </div>

        <div class="form-actions">
            <button type="submit" class="button primary">Guardar cambios</button>
            <a class="button secondary" asp-action="Index">Cancelar</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
